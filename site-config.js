/* Add only the real Fontly App Store URL. An empty value leaves the button hidden. */
window.YK_SITE_CONFIG = Object.freeze({
  apps: Object.freeze({
    fontly: Object.freeze({ appStoreUrl: '' })
  })
});
