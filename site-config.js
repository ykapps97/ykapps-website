/* Live App Store URLs. An empty value leaves the download button hidden. */
window.YK_SITE_CONFIG = Object.freeze({
  apps: Object.freeze({
    fontly: Object.freeze({ appStoreUrl: 'https://apps.apple.com/app/id6808726720' }),
    loveloop: Object.freeze({ appStoreUrl: 'https://apps.apple.com/app/id6811555270' })
  })
});
